<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign up</title>
</head>
<body>
    <form method="POST" action="/quizsystem/instruct">
        <div class="form-group">
          <label for="Username">First Name</label>
          <input type="text" class="form-control" id="username" name="username" required>
        </div>
        <div class="form-group">
          <label for="email">Email</label>
          <input type="email" class="form-control" id="email" name="email" required>
        </div>
        <div class="form-group">
          <label for="password">password</label>
          <input type="text" class="form-control" id="password" name="password" required>
        </div>
        <button type="submit" class="btn btn-primary">Sign Up</button>
      </form>
</body>
</html> -->

<!-- views/instructor-signup.ejs -->

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instructor Sign Up</title>
    <!-- Include Bootstrap CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <!-- Include any additional stylesheets or scripts you may need -->
    <style>
        body {
            background: linear-gradient(to right, #3498db, #6c5ce7);
            color: #fff;
            font-family: 'Arial', sans-serif;
        }

        .container {
            background-color: rgba(52, 152, 219, 0.9);
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 30px;
            margin-top: 50px;
        }

        h1 {
            color: #000;
        }

        form {
            max-width: 400px;
            margin: 0 auto;
        }

        label {
            color: #000;
        }

        .btn-primary {
            background-color: #6c5ce7;
            border-color: #6c5ce7;
            border-radius: 5px;
        }

        .btn-primary:hover {
            background-color: #4a3cc4;
            border-color: #4a3cc4;
        }

        .login-link {
            display: block;
            text-align: center;
            margin-top: 20px;
            color: #000;
        }
    </style>
</head>

<body>
    <div class="container mt-5">
        <h1 class="text-center mb-4">Instructor Sign Up</h1>

        <!-- Your instructor sign-up form with Bootstrap classes -->
        <form method="POST" action="/quizsystem/instruct">
            <div class="form-group">
              <label for="Username">First Name</label>
              <input type="text" class="form-control" id="username" name="username" required>
            </div>
            <div class="form-group">
              <label for="email">Email</label>
              <input type="email" class="form-control" id="email" name="email" required>
            </div>
            <div class="form-group">
              <label for="password">password</label>
              <input type="text" class="form-control" id="password" name="password" required>
            </div>
            <button type="submit" class="btn btn-primary">Sign Up</button>
          </form>

        <!-- Login link -->
        <a href="/quizsystem/login/instructor" class="login-link">Already have an account? Log in here</a>
    </div>

    <!-- Include Bootstrap JS and any additional scripts or libraries you may need -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.10.2/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.min.js"></script>
</body>

</html>
