<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Quiz</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: url(https://gamethinking.io/wp-content/uploads/sites/5/2018/12/quiz-background.png);
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        form {
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 400px;
        }

        h1 {
            text-align: center;
            color: #333;
        }

        label {
            display: block;
            margin-top: 10px;
            color: #555;
        }

        input[type="text"],
        textarea,
        input[type="number"] {
            width: 100%;
            padding: 10px;
            margin-top: 5px;
            margin-bottom: 15px;
            box-sizing: border-box;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 16px;
        }

        input[type="hidden"] {
            display: none;
        }

        .button-link {
            display: block;
            width: 100%;
            padding: 10px;
            background-color: #d1c626;
            color: #fff;
            text-align: center;
            text-decoration: none;
            font-size: 18px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        .button-link:hover {
            background-color: #45a049;
        }
    </style>
</head>

<!-- HTML Form for Creating a Quiz -->

<body>
    <form action="/quizsystem/quiz" method="post">

        <!-- Form Heading -->
        <h1>Create Quiz</h1>

        <!-- Title Input -->
        <label for="title">Title:</label>
        <input type="text" id="title" name="title" required>

        <!-- Description Textarea -->
        <label for="description">Description:</label>
        <textarea id="description" name="description" required></textarea>

        <!-- Time Limit Input -->
        <label for="timeLimit">Time Limit (minutes):</label>
        <input type="number" id="timeLimit" name="timeLimit" required>

        <!-- Instructor ID Input (Hidden) -->
        <input type="hidden" name="instructorID" value="<%= instructorID %>">

        <!-- Submit Button -->
        <button type="submit" class="button-link">Create Quiz</button>
    </form>
</body>

</html>