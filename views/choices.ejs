<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Question Choices</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: url(https://gamethinking.io/wp-content/uploads/sites/5/2018/12/quiz-background.png);
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            min-height: 100vh;
            color: #dbd5d5;
        }

        h1 {
            margin-top: 20px;
        }

        table {
            width: 80%;
            border-collapse: collapse;
            margin-top: 20px;
            background-color: #fff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            overflow: hidden;
        }

        th,
        td {
            padding: 15px;
            text-align: left;
            color: #333;
            border-bottom: 1px solid #ddd;
        }

        th {
            background-color: #d1c626;
            color: white;
        }

        tr:hover {
            background-color: #f5f5f5;
        }

        a {
            text-decoration: none;
            color: #333;
        }

        .btn-container {
            display: flex;
            gap: 10px;
        }

        .btn {
            color: rgb(27, 25, 25);
            background-color: #007bff;
            padding: 8px 12px;
            border-radius: 4px;
            cursor: pointer;
        }

        .hidden-form {
            display: none;
        }
    </style>
</head>

<body>
    <h1>Choices of the Question </h1>

    <table>
        <!-- Table Header -->
        <thead>
            <tr>
                <!-- Column Headers -->
                <th>Choice ID</th>
                <th>Choice Text</th>
                <th>Is Correct</th>
                <th>Actions</th>
            </tr>
        </thead>

        <!-- Table Body -->
        <tbody>
            <!-- Loop through each choice in the 'choices' array -->
            <% for (const choice of choices) { %>
                <!-- Table Row for Each Choice -->
                <tr>
                    <!-- Display Choice ID -->
                    <td>
                        <%= choice.ChoiceID %>
                    </td>
                    <!-- Display Choice Text -->
                    <td>
                        <%= choice.ChoiceText %>
                    </td>
                    <!-- Display Is Correct (convert 1 to true, 0 to false) -->
                    <td>
                        <%= choice.IsCorrect===1 ? 'true' : 'false' %>
                    </td>
                    <td class="btn-container">
                        <!-- Delete Button Form -->
                        <form class="btn-delete" action="/quizsystem/choices-delete/<%= choice.ChoiceID %>"
                            method="GET">
                            <button class="btn" type="submit">Delete</button>
                        </form>

                       

                    </td>
                </tr>

                <% } %>
        </tbody>
    </table>



</body>

</html>