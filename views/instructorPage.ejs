<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome </title>

    <link rel="stylesheet" href="https://bootswatch.com/4/cosmo/bootstrap.min.css">
    <script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
    <style>
        body {
            background-image: url(https://e0.pxfuel.com/wallpapers/149/259/desktop-wallpaper-web-development.jpg);
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
        }

        .navbar {
            background-color: #343a40;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .navbar-brand,
        .username,
        .email {
            color: #fff;
            padding: 10px;
            cursor: pointer;
            transition: color 0.3s ease;
        }

        .navbar-brand:hover,
        .username:hover,
        .email:hover {
            color: #45a049;
        }



        .navbar-toggler-icon {
            background-color: #fff;
        }

        .container {
            margin-top: 50px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 30px;
        }

        #welcome-message {
            color: #fff;
            font-size: 30px;
            font-weight: bold;
            margin-bottom: 20px;
        }

        .card {
            border: 1px solid #ddd;
            border-radius: 50px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
            width: 18rem;
            margin: 0 auto;
            transition: transform 0.3s;
            background-color: #1b1a1a;
        }

        .card:hover {
            transform: scale(1.05);
        }

        .card-title {
            color: #cac8a8;
            font-size: 18px;
            font-weight: bold;
        }

        .card-text {
            color: #98a2aa;
            font-size: 14px;
        }

        .btn-primary {
            background-color: #272f38;
            border-radius: 13px;
        }

        .btn-primary:hover {
            background-color: #27261b;
            border-color: #b6c3d1;
            color: #fff;
        }

        .popup-container {
            display: none;
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            padding: 20px;
            background-color: #fff;
            border: 1px solid #ccc;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            z-index: 1000;
        }

        .overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            z-index: 999;
        }

        .close-btn {
            position: absolute;
            top: 10px;
            right: 10px;
            cursor: pointer;
        }

        .popup-container:target,
        .overlay:target {
            display: block;
        }

        #settingsModal {
            background: rgba(0, 0, 0, 0.5);
        }

        #settingsModalLabel {
            color: #f0f0f0;
        }

        .modal-content {
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
        }

        .modal-header {
            background-color: #455769;
            border-color: #007bff;
            color: #fff;
            border-bottom: 1px solid #80abd6;
            border-radius: 10px 10px 0 0;
            padding: 15px;
        }

        .modal-title {
            font-size: 20px;

        }

        .close {
            color: #fff;

            opacity: 1;

        }

        .close:hover {
            color: #f70808;
        }

        .modal-body {
            padding: 20px;
        }
    </style>

</head>

<body>


    <!-- Navigation Bar for Instructor Page -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">

        <!-- Navbar Brand - Instructor Page -->
        <li class="navbar-brand">Instructor Page</li>

        <!-- User Information - Username and Email -->
        <li class="username"> Username : <%= instructorDetails.instructorDetails.Username %>
        </li>
        <li class="email"> Email: <%= instructorDetails.instructorDetails.Email %>
        </li>

        <!-- Navbar Toggler Button for Responsive Design -->
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Navbar Collapse Section -->
        <div class="collapse navbar-collapse" id="navbarNav">

            <!-- Navbar Navigation Items -->
            <ul class="navbar-nav ml-auto">

                <!-- View Quizzes Link -->
                <li class="nav-item active">
                    <a class="nav-link"
                        href="/quizsystem/my-quizzes/<%= instructorDetails.instructorDetails.InstructorID %>">View
                        Quizzes</a>
                </li>

                <li class="nav-item active">
                    <a class="nav-link" href="/quizsystem/students %>">View
                        students</a>
                </li>


                <!-- Dropdown Menu for Instructor Actions -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" id="navbarDropdown" role="button" data-toggle="dropdown"
                        aria-haspopup="true" aria-expanded="false">
                        Instructor Menu
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">

                        <!-- Settings Option with Modal Trigger -->
                        <a class="dropdown-item" href="#" data-toggle="modal" data-target="#settingsModal">Settings</a>

                        <!-- Divider Line in Dropdown Menu -->
                        <div class="dropdown-divider"></div>

                        <!-- Logout Option -->
                        <a class="dropdown-item" href="/quizsystem/logout">Logout</a>
                    </div>
                </li>

            </ul> <!-- End of Navbar Navigation Items -->

        </div> <!-- End of Navbar Collapse Section -->

    </nav> <!-- End of Navigation Bar for Instructor Page -->


    <h1 id="welcome-message" class="text-center mb-4">Welcome <%=instructorDetails.instructorDetails.Username%>
    </h1>


    <!-- Row with Three Cards - Text Centered -->
    <div class="row text-center">

        <!-- Card 1: Create a Quiz -->
        <div class="col-md-12">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Create a Quiz</h5>
                    <p class="card-text">Build a new quiz for your students.</p>
                    <a href="/quizsystem/create-quiz" class="btn btn-primary">Create</a>
                </div>
            </div>
        </div>

        
        <!-- Card 2: Add a Small Container -->
        <div class="col-md-12">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Post an Announcement</h5>
                    <p class="card-text">Send an announcement for a student.</p>
                    <a href="/quizsystem/send-announ/<%=instructorDetails.instructorDetails.InstructorID %>"
                        class="btn btn-primary">Post</a>
                </div>
            </div>
        </div>

    </div> <!-- End of Row with Three Cards - Text Centered -->


    <!-- Settings Modal -->
    <div class="modal fade" id="settingsModal" tabindex="-1" role="dialog" aria-labelledby="settingsModalLabel"
        aria-hidden="true">

        <!-- Modal Dialog with Scrollable Content -->
        <div class="modal-dialog modal-dialog-scrollable" role="document">

            <!-- Modal Content -->
            <div class="modal-content">

                <!-- Modal Header with Title and Close Button -->
                <div class="modal-header">
                    <h5 class="modal-title" id="settingsModalLabel">Settings</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>

                <!-- Important Note Section -->
                <h1 style="font-size: 1em; font-weight: bold;">Updating the following will take you back to the login
                    page</h1>

                <!-- Modal Body -->
                <div class="modal-body">

                    <!-- Update Account Form -->
                    <h1>Update Account</h1>
                    <form action="/quizsystem/update-instructor/<%=instructorDetails.instructorDetails.InstructorID %>"
                        method="POST" id="updateAccountForm">

                        <!-- New Name Input -->
                        <div class="form-group">
                            <label for="newName">New Name</label>
                            <input type="text" class="form-control" id="username" name="username">
                        </div>

                        <!-- New Email Input -->
                        <div class="form-group">
                            <label for="newEmail">New Email</label>
                            <input type="email" class="form-control" id="email" name="email">
                        </div>

                        <!-- New Password Input -->
                        <div class="form-group">
                            <label for="newPassword">New Password</label>
                            <input type="password" class="form-control" id="password" name="password">
                        </div>

                        <!-- Submit Button -->
                        <button type="submit" class="btn btn-primary">Update Account</button>
                    </form>

                    <!-- Change Password Form -->
                    <h1>Change Password</h1>
                    <form
                        action="/quizsystem/update-instructor/pass/<%=instructorDetails.instructorDetails.InstructorID %>"
                        method="POST" id="changePasswordForm">

                        <!-- New Password Input -->
                        <div class="form-group">
                            <label for="newPassword">New Password</label>
                            <input type="password" class="form-control" id="newPassword" name="newPassword">
                        </div>

                        <!-- Submit Button -->
                        <button type="submit" class="btn btn-primary">Change Password</button>
                    </form>

                </div> <!-- End of Modal Body -->

            </div> <!-- End of Modal Content -->

        </div> <!-- End of Modal Dialog -->

    </div> <!-- End of Settings Modal -->



    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.10.2/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.min.js"></script>
</body>

</html>