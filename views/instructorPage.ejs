<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome </title>

    <link rel="stylesheet" href="https://bootswatch.com/4/cosmo/bootstrap.min.css">
    <script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
    <style>
        body {
            background-image: url(https://e0.pxfuel.com/wallpapers/149/259/desktop-wallpaper-web-development.jpg);
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
        }

        .navbar {
            background-color: #343a40;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .navbar-brand {
            font-size: 1.5rem;
        }

        .navbar-nav .nav-link {
            color: #fff !important;
        }

        .navbar-nav .nav-link:hover {
            color: #007bff !important;
        }

        .navbar-toggler-icon {
            background-color: #fff;
        }

        .container {
            margin-top: 50px;
            background-color: rgba(120, 182, 166, 0.932);
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 30px;
        }

        #welcome-message {
            color: rgb(182, 190, 188);
            position: relative;
            right: 2%;
            font-size: 30px;
            font-weight: bold;
            margin-bottom: 20px;
        }

        .card {
            border: 1px solid #ddd;
            border-radius: 50px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
            position: relative;
            left: 500px;
            width: 18rem;
            margin-bottom: 20px;
            transition: transform 0.3s;
            background-color: #1b1a1a;
        }

        .card:hover {
            transform: scale(1.05);
        }

        .card-title {
            color: #cac8a8;
            font-size: 18px;
            font-weight: bold;
        }

        .card-text {
            color: #98a2aa;
            font-size: 14px;
        }

        .btn-primary {
            background-color: #272f38;
            border-radius: 13px;

        }

        .btn-primary:hover {
            background-color: #27261b;
            border-color: #b6c3d1;
            color: #fff;
        }
    </style>
</head>

<body>


    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <li class="navbar-brand">Instructor Page</li>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="/quizsystem/students">Display All Students</a>
                </li>

                <!-- Add a dropdown menu with options -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" id="navbarDropdown" role="button" data-toggle="dropdown"
                        aria-haspopup="true" aria-expanded="false">
                        Instructor Menu
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <!-- Add profile image and other options -->
                        <a class="dropdown-item" href="#">Profile</a>
                        <a class="dropdown-item" href="#" data-toggle="modal" data-target="#settingsModal">Settings</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="#">Logout</a>
                    </div>
                </li>
            </ul>
        </div>
    </nav>

    <h1 id="welcome-message" class="text-center mb-4">Welcome <%=instructorDetails.instructorDetails.Username%>
    </h1>

    <div class="row text-center">
        <!-- Card 1: Create a Quiz -->
        <div class="col-md-12">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Create a Quiz</h5>
                    <p class="card-text">Build a new quiz for your students.</p>
                    <a href="/quizsystem/create-quiz" class="btn btn-primary">Create</a>
                </div>
            </div>
        </div>

        <!-- Card 2: Check Results for a Specific Quiz -->
        <div class="col-md-12">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Check Quiz Results</h5>
                    <p class="card-text">View results for a specific quiz.</p>
                    <a href="#" class="btn btn-primary">Check Results</a>
                </div>
            </div>
        </div>

        <!-- Card 3: Add a Small Container -->
        <div class="col-md-12">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">Instructor Info</h5>
                    <p class="card-text">View and update your instructor information.</p>
                    <a href="#" class="btn btn-primary">View Info</a>
                </div>
            </div>
        </div>
    </div>
    </div>


    <div class="modal fade" id="settingsModal" tabindex="-1" role="dialog" aria-labelledby="settingsModalLabel"
        aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="settingsModalLabel">Settings</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <!-- Delete Account Form -->
                    <h1>DELETE </h1>
                    <form action="/quizsystem/instructor-delete/<%= instructorDetails.instructorDetails.InstructorID %>"
                        method="GET" id="deleteAccountForm">

                        <ul class="list-group">
                            <li class="list-group-item">
                                <p>Are you sure you want to delete your account?</p>
                                <button type="submit" class="btn btn-danger">Delete Account</button>
                            </li>
                        </ul>
                    </form>

                    <!-- Update Account Form -->
                    <h1>UPDATE</h1>
                    <form action="/quizsystem/update-instructor/<%=instructorDetails.instructorDetails.InstructorID %>"
                        method="POST" id="updateAccountForm">
                      <!-- Replace the placeholders with actual form elements -->
                      <div class="form-group">
                          <label for="newName">New Name</label>
                          <input type="text" class="form-control" id="username" name="username"> <!-- Ensure the name attribute is "username" -->
                      </div>
                  
                      <div class="form-group">
                          <label for="newEmail">New Email</label>
                          <input type="email" class="form-control" id="email" name="email"> <!-- Ensure the name attribute is "email" -->
                      </div>
                      <div class="form-group">
                          <label for="newPassword">New Password</label>
                          <input type="password" class="form-control" id="password" name="password"> <!-- Ensure the name attribute is "password" -->
                      </div>
                      <button type="submit" class="btn btn-primary">Update Account</button>
                  </form>
                  


                    <!-- Change Password Form -->
                    <form action="/change-password" method="POST" id="changePasswordForm">
                        <!-- Replace the placeholders with actual form elements -->
                        <div class="form-group">
                            <label for="newPassword">New Password</label>
                            <input type="password" class="form-control" id="newPassword" name="newPassword">
                        </div>
                        <button type="submit" class="btn btn-primary">Change Password</button>
                    </form>
                </div>
            </div>
        </div>
    </div>


    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.10.2/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.min.js"></script>
</body>

</html>